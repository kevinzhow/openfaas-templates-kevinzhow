provider:
  name: openfaas
  gateway: http://127.0.0.1:8080  # can be a remote server

functions:
  # returns what you send to
  hello-swift:
    lang: dockerfile
    image: kevinzhow/hello-swift:latest
    skip_build: true